# Brand mark variants (A/B)

Canonical mark is `../mark-{light,dark}.svg`:
**BOM with agent O** (bill-of-materials wordmark where O is the agent face).

Optional alternatives kept for review:

| File | Concept |
|---|---|
| `b-agent-materials-{light,dark}.svg` | Prompt block + materials list |
| `c-brace-bom-{light,dark}.svg` | Code braces framing dependency ticks |

Accent: emerald → cyan. Edit `docs/images/brand/` first, then copy to
`ui/public/brand/` and regenerate favicon / site-docs marks.
