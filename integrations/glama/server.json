{
  "name": "agent-bom",
  "description": "Security scanner for AI infrastructure — CVEs, blast radius, AI BOM, compliance, 16 output formats",
  "version": "0.70.7",
  "license": "Apache-2.0",
  "repository": "https://github.com/msaad00/agent-bom",
  "transport": "stdio",
  "command": "agent-bom",
  "args": ["mcp-server"],
  "dockerfile": "integrations/glama/Dockerfile",
  "environmentVariables": {
    "NVD_API_KEY": {
      "description": "NVD API key for higher rate limits on vulnerability enrichment",
      "required": false
    }
  }
}
